import { DashboardRenderGridCell } from './dashboard-render-grid-cell';

describe('DashboardRenderGridCell', () => {
  it('should create an instance', () => {
    expect(new DashboardRenderGridCell()).toBeTruthy();
  });
});
