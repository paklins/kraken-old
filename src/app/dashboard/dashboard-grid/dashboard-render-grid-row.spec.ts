import { DashboardRenderGridRow } from './dashboard-render-grid-row';

describe('DashboardRenderGridRow', () => {
  it('should create an instance', () => {
    expect(new DashboardRenderGridRow()).toBeTruthy();
  });
});
