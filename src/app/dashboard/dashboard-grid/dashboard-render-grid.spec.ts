import { DashboardRenderGrid } from './dashboard-render-grid';

describe('DashboardRenderGrid', () => {
  it('should create an instance', () => {
    expect(new DashboardRenderGrid()).toBeTruthy();
  });
});
