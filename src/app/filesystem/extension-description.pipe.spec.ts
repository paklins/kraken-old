import { ExtensionDescriptionPipe } from './extension-description.pipe';

describe('ExtensionDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
